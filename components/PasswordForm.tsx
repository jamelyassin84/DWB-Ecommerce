import React, { FC } from 'react'
import { Text } from 'react-native'
import Container from './Layout'

type Props = {}

const PasswordForm: FC<Props> = (props) => {
	return (
		<Container>
			<Text>PasswordForm</Text>
		</Container>
	)
}

export default PasswordForm
