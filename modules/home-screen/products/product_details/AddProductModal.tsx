import React, { FC } from 'react'
import { Text } from 'react-native'
import Container from '../../../../components/app/Layout'

type Props = {}

const AddProductModal: FC<Props> = (props) => {
	return (
		<Container>
			<Text>AddProductModal</Text>
		</Container>
	)
}

export default AddProductModal
