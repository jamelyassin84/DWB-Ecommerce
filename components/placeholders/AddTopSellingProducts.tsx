import React, { FC } from 'react'
import { Text } from 'react-native'
import { View } from '../overrides/Themed'

type Props = {}

const AddTopSellingProducts: FC<Props> = (props) => {
	return (
		<View>
			<Text>AddTopSellingProducts</Text>
		</View>
	)
}

export default AddTopSellingProducts
