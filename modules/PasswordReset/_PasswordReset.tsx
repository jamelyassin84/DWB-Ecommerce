import React, { FC } from 'react'
import { Text } from 'react-native'
import Container from '../../components/Layout'

type Props = {}

const _PasswordReset: FC<Props> = (props) => {
	return (
		<Container>
			<Text>_PasswordReset</Text>
		</Container>
	)
}

export default _PasswordReset
