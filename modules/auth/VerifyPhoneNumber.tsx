import React, { FC } from 'react'
import { Text } from 'react-native'
import Container from '../../components/app/Layout'

type Props = {}

const VerifyPhoneNumber: FC<Props> = (props) => {
	return (
		<Container>
			<Text>VerifyPhoneNumber</Text>
		</Container>
	)
}

export default VerifyPhoneNumber
